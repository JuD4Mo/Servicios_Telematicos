#Se niega el acceso a usuarios que estén en la lista, en este caso /etc/denied
auth required pam_listfile.so item=user sense=deny file=/etc/apache2/usuariosDenegados onerr=fail

#Autenticación de manera normal contra usuarios del sistema
auth required pam_unix.so

#Se comprueba la cuenta
account required pam_unix.so